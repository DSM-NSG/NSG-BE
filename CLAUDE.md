# NSG-BE 개발 규칙

## 새 기능 개발 워크플로우

새 기능을 구현하기 전에 반드시 아래 절차를 따른다.

### 1단계 — plan.md 작성 (구현 전)

기능 요청을 받으면 **즉시 구현하지 말고**, 프로젝트 루트에 `plan.md` 파일을 작성한다.

`plan.md`에 포함해야 할 내용:
- **기능 개요**: 무엇을 만드는가
- **변경 파일 목록**: 신규 생성 / 수정 / 삭제할 파일 경로
- **모델 변경**: 새 필드/모델/마이그레이션 여부
- **API 엔드포인트**: 추가되는 URL, 메서드, 요청/응답 스키마
- **서비스 로직**: 핵심 비즈니스 로직 설명
- **구현 순서**: 단계별 작업 순서 (의존성 고려)
- **고려사항**: 엣지 케이스, 보안, 성능 이슈

작성 후 사용자에게 검토를 요청한다. 승인 전까지 코드를 작성하지 않는다.

### 2단계 — 사용자 검토

사용자가 `plan.md`를 검토하고 피드백을 준다.
- 수정 요청이 있으면 `plan.md`를 업데이트하고 다시 검토를 요청한다.
- 승인("진행해", "ok", "구현해" 등)이 오면 3단계로 진행한다.

### 3단계 — 구현

`plan.md`의 구현 순서에 따라 코드를 작성한다.
- 각 단계가 완료될 때마다 진행 상황을 알린다.
- 구현 완료 후 `plan.md`를 삭제하거나 `plan.md`에 완료 표시를 한다.

---

## API 명세 관리

새 API를 구현할 때마다 `api-spec/` 디렉토리에 버전 파일을 추가한다.

### 버전 네이밍 규칙 (Semantic Versioning)
```
api-spec/
├── v1.0.0.json    # 최초 릴리즈
├── v1.1.0.json    # 새 기능 추가 (minor)
├── v1.1.1.json    # 버그 수정 / 스펙 오류 수정 (patch)
└── v2.0.0.json    # 하위 호환 불가 변경 (major)
```

- **major**: 기존 API 삭제 또는 하위 호환 불가 변경
- **minor**: 새 엔드포인트 추가
- **patch**: 스펙 오류 수정, 설명 변경 등

### 규칙
- 새 API 구현 완료 시 이전 버전을 복사해 버전을 올린 새 파일을 생성하고 변경사항을 추가한다.
- `api-spec.json` (루트)은 항상 최신 버전과 동일하게 유지한다.
- 각 파일은 완전한 OpenAPI 3.0.3 스펙을 포함한다 (참조가 아닌 전체 내용).

---

## 코드 컨벤션

이 프로젝트의 구조를 따른다:
- 각 앱은 `models/`, `apis/`, `service/` 디렉토리로 분리
- 비즈니스 로직은 반드시 `service/` 레이어에 작성
- 뷰는 얇게 유지 (직렬화 + 서비스 호출만)
- 모든 UUID는 PK로 사용
- 한국어 에러 메시지 사용 (CustomAPIException 상속)
- 트랜잭션이 필요한 작업은 `transaction.atomic()` 사용
